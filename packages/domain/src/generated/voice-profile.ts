/* eslint-disable */
// GENERATED by tools/gen-types.ts from schemas/*.schema.json — do not edit. Run `pnpm gen:types`.

/**
 * ADR-0083 (C3): the operator's measured voice as rule lines in the manuscript language, derived from their own published chapters (docs/10-corpus). A policy names a profile (identity.voice_profile); at novel start its lines are copied into the project's composed identity (preferences.operator_voice), so a project reads frozen bytes. Writers and editors read `writer`, planners `planner`, judges `judges` (the operator's conventions that are not defects). Versions are immutable.
 */
export interface OperatorVoiceProfile {
  id: string;
  version: number;
  /**
   * The manuscript language. Composition is direct in this language (ADR-0054); English is an explicit later translation surface. Enforced per project by the identity contract.
   */
  language: 'en' | 'ko';
  name: string;
  /**
   * Where the numbers and conventions come from (documents, corpus scope).
   */
  evidence: string;
  /**
   * @minItems 1
   */
  writer: [string, ...string[]];
  planner: string[];
  judges: string[];
  content_hash?: string;
}
